# Знакомство с работой psycopg2
# pip install psycopg2
import psycopg2

# Создаем переменную для хранения параметров DB
# database_config = {'host': '', 'dbname': '', 'user': '', 'password': ''}, значения не заполнены

# Создаем подключение
# conn = psycopg2.connect(**database_config)

# Создаем курсор
# cursor = conn.cursor()

# Можем использовать execute
# cursor.execute('') -- нужно добавить запрос

# Вывод всех строк из запроса или одной строки
# cursor.fetchall() or fetchone()
